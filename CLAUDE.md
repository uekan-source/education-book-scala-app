# このリポジトリについて

Scala 3 + DDD の研修用リポジトリ。バーガーチェーンの受注システムを題材に、要件定義からモデル設計までを演習する。

**`docs/notes/` が自分の作業用、`docs/reference/` が原典（教材の写し）。** 手順は notes 側だけを見れば完結する。

| ファイル | 何が書いてあるか |
|---|---|
| **`docs/notes/design_procedure.md`** | **手順。設計を始めるときはこれを最初から最後まで読む。** テンプレートとチェックリストも中にある |
| `docs/notes/document_format.md` | 成果物の書き方（`01`/`02`/`03` に何を書くか） |
| `docs/notes/design_lessons.md` | 原則（なぜそうするか）。つまずいた実例つき |
| `docs/notes/sql_basics.md` | このプロジェクトで必要な範囲の SQL |
| `docs/reference/README.md` | **原典の索引。** 教材と雛形の写し |

**`docs/reference/` は、設計の判断を答える前に見る。迷ったときだけではない。** ── **推測で答えられてしまう問いほど、資料に理由が書かれている**（`shopId` を「集計のため」と推測して外した実例がある。正解はアクセス境界の鍵で、後から足せない列だった）

## ドキュメントの構成

機能ごとに `docs/domain/<機能名>/` を作り、3 ファイルで進める。

```
01_requirements.md  受け取った要求 / 質問リスト / 業務フローと例外ルール
02_analysis.md      思考過程のメモ（提出しない。当時の判断の記録として残す）
03_design.md        最終的な設計。人に見せるのはこれ
```

`02` は**書き換えない**。その時点でどう考えたかの記録なので、後の変更を反映すると価値が失われる。

## モデルを増やすとき

**手順は `docs/notes/design_procedure.md`。** 設計を始めるときは、まずそれを開く。

要点だけ。

- **要件定義とモデル設計は並行して回す。** 3 ファイルを行き来して何周もする。**1 周目で完成形を目指さない**
- **アクターが先、業務フローが後。** 先にフローを書くと、書いたフローの主語しか登場しない
- 1 機能追加のときは **「概念を配る」と「既存フローに流す」が本番**
- **穴が出たら `01` の質問リストに戻る。** 設計は要件を検証する作業でもある。**穴が出ないほうが疑わしい**

**手順を回したら `design_procedure.md` の「使ってみて分かったこと」に追記する。** 重かった段階、足りなかった観点を書き足して、手順自体を育てる。

## 設計文書（03）に必ず書くもの

- **型では守れない決めごと** — 一意制約、属性どうしの連動、運用ルール。モデルは形だけを書き、ロジックは載せない
- **保存されるデータの例** — パターンごとに 1 行ずつ、実際に入る値で
- **付録B 判断の記録** — 論点ごとに「判断 / 理由 3〜6 行 / 代償 / 判断が変わる条件」。迷わなかったことは書かない

## 図

- 状態遷移図とフローチャートは **SVG を手で書いて** `docs/domain/<機能名>/images/` に置く。mermaid は自動レイアウトで線が重なるため使わない
- ER 図は mermaid のままでよい
- **図の中に章番号を書いたら、本文の再編時に図も直す**。マークダウンだけ直して図を忘れやすい

## 命名

- テーブル名はコンテキストのパスを頭に付け、単数形（`sales_user_coupon`、`common_coupon`）
- 区分値は `IS_` 始まり。`code` は正が「生きている」、負が「終わった」

## AI に文書を書かせるとき

**1 回のお願いで清書までやらせない。** 段階ごとに頭の使い方が違うので、混ぜると必ず崩れる。

| 段階 | 頭の使い方 | やること |
|---|---|---|
| **① 調査中** | **散らかしてよい** | 分かったことを、どんどん走り書きする |
| **② 渡す前** | **読者の目で組み直す** | 構成・用語・粒度をそろえる |
| **③ 最後** | **ノイズを消す** | AI っぽさ・過剰な装飾・未確定情報を削る |

**「きれいにして」と一言で頼まない。** ①②③ のどれかを指定する。指定がないと、たいてい **③（見た目の整形）だけが返り、②（構成と粒度）が手付かずで残る。**

## 作業の進め方

- 文書を直したら**必ず整合性を確認する**。相互参照が密なので、1 箇所の修正が別の箇所と噛み合わなくなる
- 矛盾が繰り返し出るときは、個々の矛盾ではなく**ルールの切り分け方**を疑う
- レビューは「指摘」と「検証」を分ける。指摘をそのまま受け入れず、反証を試みてから採用する
